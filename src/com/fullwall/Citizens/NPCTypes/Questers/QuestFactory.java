package com.fullwall.Citizens.NPCTypes.Questers;

public class QuestFactory {
	/*
	 * public static Quest newParsedQuest(HumanNPC npc, Player player, QuestType
	 * type, String string) { switch (type) { case BUILD: return new
	 * BuildQuest(npc, player).parse(string); case COLLECT: return new
	 * CollectQuest(npc, player).parse(string); case DELIVERY: return new
	 * DeliveryQuest(npc, player).parse(string); case DESTROY_BLOCK: return new
	 * DestroyQuest(npc, player).parse(string); case EARN: return new
	 * EarnQuest(npc, player).parse(string); case HUNT: return new
	 * HuntQuest(npc, player).parse(string); case MOVE_DISTANCE: return new
	 * DistanceQuest(npc, player).parse(string); case MOVE_LOCATION: return new
	 * LocationQuest(npc, player).parse(string); case PLAYER_COMBAT: return new
	 * CombatQuest(npc, player).parse(string); default: return null; } }
	 */
}
